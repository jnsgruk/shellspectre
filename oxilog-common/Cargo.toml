[package]
name = "oxilog-common"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[features]
default = []
std = ["serde"]

[dependencies]
serde = { version = "1", default-features = false, features = ["derive"], optional = true }
