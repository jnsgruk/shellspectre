summary: Read event captures stdin
execute: |
  echo 'shspectr-stdin-marker' | /usr/bin/dd bs=100 count=1 2>/dev/null

  sqlite3 "$DB_PATH" \
    "SELECT data FROM events WHERE event_type = 'read'" \
    | MATCH shspectr-stdin-marker
